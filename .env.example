# Backend API gateway URL (used by Vite dev proxy)
BACKEND_URL=http://localhost:8000

# Public backend URL for production (exposed to browser)
# In dev, leave unset — Vite proxy handles /api/* → BACKEND_URL
# In production (Vercel), set to your actual backend URL e.g. https://api.whatsdo.com
VITE_API_URL=
